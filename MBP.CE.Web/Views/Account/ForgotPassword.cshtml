@model MBP.CE.Web.Models.ForgotPasswordViewModel
@{
    ViewBag.Title = @Resources.ChangePassword;
}

<div class="row">
    <div class="col-md-2"></div>
    <div class="col-md-8 text-center">
        <h3 style="margin-bottom: 30px">@Resources.ForgotPassword</h3>

        @using (Html.BeginForm("ForgotPassword", "Account", new { ReturnUrl = ViewBag.ReturnUrl }, FormMethod.Post, new { @class = "form center-block text-left" }))
        {
            @Html.AntiForgeryToken()

            if (ViewData.ModelState.Values.Any(x => x.Errors.Any()))
            {
                <div class="alert alert-danger">
                    <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                    @Html.ValidationSummary(false, "", new { @class = "" })
                </div>
            }

            <div class="row row-margin-bottom">
                <div class="col-md-2"></div>
                <div class="col-md-3">
                    @Html.LabelFor(m => m.Username)
                </div>
                <div class="col-md-5">
                    @Html.TextBoxFor(m => m.Username, new { @class = "width_100p" })
                </div>
                <div class="col-md-2"></div>
            </div>

            <div class="row text-center" style="margin-top: 20px;">
                <button class="highlight" style="width: 170px !important;">@Resources.SendPassword</button>
            </div>
        }
    </div>
    <div class="col-md-2"></div>
</div>